
\guideline[g:typography:variable_indices]
    {Use italics for variable indices/superscripts and roman font for indices/superscripts that abbreviate words.}

\goodbadexample[{\cite[Theorem 2]{Wetzlinger2025TAC}}]{
    The union of time-point EA backward reachable sets
    \begin{equation*}
        \bigcup_{t\in\tau_k} \mathcal{R}_{\exists\forall}(-t) = \{ e^{-At} \big( ( \mathcal{X}_{\highlightpartmath{end}} \ominus \mathcal{Z}_{\mathcal{W}}(t) ) \oplus - \mathcal{Z}_{\mathcal{U}}(t)\big) \, | \, t \in \tau_{\highlightpartmath{\text{k}}} \}
    \end{equation*}
    over $\tau_k = [t_k, t_{k+1}]$ can (...)
}{
    The union of time-point EA backward reachable sets
    \begin{equation*}
        \bigcup_{t\in\tau_k} \mathcal{R}_{\exists\forall}(-t) = \{ e^{-At} \big( ( \mathcal{X}_{\highlightpartmath{\text{end}}} \ominus \mathcal{Z}_{\mathcal{W}}(t) ) \oplus - \mathcal{Z}_{\mathcal{U}}(t)\big) \, | \, t \in \tau_{\highlightpartmath{k}} \}
    \end{equation*}
    over $\tau_k = [t_k, t_{k+1}]$ can (...)
    \goodexpl{The index ``end'' refers to the word ``end'', while the index ``k'' refers to the variable time step $k$.}
}

\noindent
Double check the formatting when using macros, as some \LaTeX{} environments change the font from roman to italics.
